import 'package:flutter/material.dart';

class StudSubjectPage extends StatelessWidget {
  const StudSubjectPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(),
      body: SafeArea(
        child: Column(children: [
          
        ],)
      )
    );
  }
}